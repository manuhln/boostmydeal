# LiveKit Configuration
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=your_livekit_api_key
LIVEKIT_API_SECRET=your_livekit_api_secret

# Server Configuration
SERVER_URL=https://your-replit-url.repl.co

# LiveKit SIP Trunk Configuration
# IMPORTANT: Get this from LiveKit Cloud dashboard or CLI: lk sip outbound list
# Example: ST_xxxxxxxxxxxxxx
# You MUST configure a SIP trunk in LiveKit Cloud for outbound calling
LIVEKIT_SIP_TRUNK_ID=ST_your_sip_trunk_id

# Webhook Configuration (Optional)
# URL to receive call events (PHONE_CALL_CONNECTED, LIVE_TRANSCRIPT, PHONE_CALL_ENDED)
# If not set, you can still pass webhook_url in the API request
WEBHOOK_URL=https://your-webhook-url.com/endpoint

# Session Secret
SESSION_SECRET=your_random_secret_key_here

# Note: TTS, STT, and LLM API keys are provided per-call via the API payload
# This allows for dynamic provider selection for each call
